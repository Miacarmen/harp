// if logged in user is the owner or admin of the event, they can update or delete the event    

// if user is not logged in, they cannot make a reservation and will be redirected to the login page
// if user is logged in, they can reserve an event
// if user is logged in, they can cancel their reservation
// if user is logged in, they can view their reservations
// if user is logged in, they can view their profile
// if user is logged in, they can update their profile
// if user is logged in, they can delete their profile
// if user is logged in, they can view all events
// if user is logged in, they can view a single event

